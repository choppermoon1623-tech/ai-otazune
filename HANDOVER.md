# 引継ぎメモ — AIおたずね箱

作った本人が忘れたとき、または別の人が続きをやるときに、**最初に読む1枚**。
「何がどこにあるか」は [README.md](README.md) に書いてある。ここに書くのは
**なぜそうしたか**と、**踏んだ落とし穴**。コードを読んでも分からないのはそこだけなので。

最終更新 2026-09-19

---

## 1. このアプリは何を解こうとしたのか

生徒がAIを使えない。ならば「使わせない」か「教師が間に入る」かの二択で、後者を選んだ。

ただし**代行そのものは目的ではない**。丸投げを受け付ける窓口を作っても、教育的には何も残らない。
だから設計の重心は、AIの呼び出しではなく**入力フォームの構造**に置いてある。

- 生徒は「知りたいこと」だけでは出せない
- 「なぜ知りたいか」「自分で調べたこと」「どこで行きづまったか」を書かせる
- AIに渡す文は、その項目からそのまま組み立てる（`buildPrompt`）

**ここを崩すと、このアプリはただのAI代行窓口になる。** 機能追加のときは必ずここを守ること。

プロンプト側の共通指示も同じ意図で入れてある。とくに
「**そのまま提出できる完成品は書かない**」「**確かめ方を必ず添える**」の2つは外さないこと。

---

## 2. ユーザー（先生）が選んだ方針

作るときに聞いて決めた。作り直すときも、ここは動かさない前提で考える。

| 論点 | 選んだもの | 捨てたもの |
|---|---|---|
| 生徒→先生の経路 | **クラウド同期（Firestore）** | QR手渡し・紙・コピペ提出・教師機1台に集約 |
| AIの呼び方 | **切替式**（既定はコピー方式、APIキーがあるときだけ自動） | 自動のみ・コピーのみ |
| 依頼の型 | **4つ全部**（調べ学習／作文／アイデア／自由） | 絞る |

コピー方式を既定にしたのは、費用がゼロなのと、**先生が必ず中身に目を通すことになる**から。
自動方式は実装してあるが、2026-09 時点で先生は使っていない。消さずに残してある。

---

## 3. 踏んだ落とし穴（同じ穴に落ちないために）

### ★ Firestore のルールはフィルタではない

**このアプリで唯一、実害が出たバグ。** 教師側の一覧が空のままだった。

ルールは「権限のある文書だけ返す」ということを**しない**。クエリを見て、
権限違反の文書を返す**可能性があるか**だけで判定し、可能性があれば丸ごと拒否する。

- 教師側は `reqs` を無条件で購読していた → 実際には自分の分しか無くても拒否される
- 生徒側は `where(authorUid == 自分)` を付けていた → 通っていた
- だから「生徒は出せたのに先生に届かない」という出方になった

対処は2つセット。**片方だけでは直らない。**

1. 教師の購読にも `where("ownerUid","==",自分)` を付ける
2. ルールの `allow list` を**先生用・生徒用の2本に分ける**。
   `||` 1本にまとめると list では証明できない（`get` は実際の値を見るので `||` のままでよい）

`scratchpad/smoke/test_otazune.js` に、クエリとルールの対応をソースレベルで固定する検査を入れてある。

### 新規Firebaseプロジェクトは Authentication を「始める」必要がある

プロバイダを有効にする前に、Authentication のページで一度「始める」を押さないといけない。
飛ばすと `auth/configuration-not-found`。CLI からは `firebase auth:export` が
`CONFIGURATION_NOT_FOUND` を返すので、**これで状態を確認できる**。

承認済みドメインも新規プロジェクトには入っていない。`choppermoon1623-tech.github.io` を
手で追加する（ホスト名のみ。`https://` もパスも付けない）。

### Firestore のリージョンは後から変えられない

CLI に任せると `nam5`（US）に作られる。日本の学校のデータなので `asia-northeast1` に作り直した。
**空のうちでないと直せない。** 作り直すときは `(default)` の再利用に約5分の待ちがある。

### jsdom には `navigator.clipboard` が無い

コピー成功時だけ「対応中」にしていたら、フォールバック経路で状態が変わらなかった。
try/catch の**外**に出した。学校のPCでも clipboard が使えないことがあるので、実機でも起きうる話。

### ブロック置換の残骸

`submit-qr` のときと同じ轍。関数ブロックを丸ごと差し替えるとき、終端マーカーに
古いコードの末尾を含め忘れると残骸が生きて `SyntaxError` になる。置換後は必ず構文チェックまで通す。

---

## 4. 検査のしかた

```
npm install jsdom            # scratchpad で1回だけ
node scratchpad/smoke/test_otazune.js
```

生徒用と教師用の jsdom を**同時に立ち上げ、共通の偽Firestoreでつないで実際にやりとりさせる**。
2026-09-19 時点で127項目。

**この検査で分かること**
受付の作成、生徒の送信、先生の対応、答えの到達、開閉、見送り、他人の分が見えないこと、
プロンプトの中身、API通信の形（ヘッダ・モデル・ストリーム解釈・エラー文言）、
ログイン失敗の案内文、クエリとルールの対応。

**この検査で分からないこと（ここは実機でやる）**
- **本物のセキュリティルールの挙動。** 偽Firestoreは要点を真似ているだけ。
  本気で確かめるなら Firestore エミュレータが要るが、**Java が必要**で、この環境には入っていない
- カメラ・QRの読み取り
- 実際のAI応答の中身

jsdom は ES module script を実行しないので、`window.fbq` は `beforeParse` で偽物を刺している。
Firebase の実装を変えたら、偽物側も合わせること。

---

## 5. 一般公開するなら（未実施・検討だけ済み）

校内数人なら現状でよい。全市・全道に配るなら、先生と相談した結果こうなった。

| やること | 状態 | なぜ |
|---|---|---|
| 専用Firebaseプロジェクト | **済**（2026-09-19） | 無料枠はプロジェクト単位。教務手帳と共倒れさせない |
| 氏名欄そのものを廃止 | 未 | 設定で選べる形だと、既定のまま氏名ありで使う先生が必ず出る |
| TTLで90日自動削除 | 未 | いまは受付を消すまで残り続ける。`expireAt` は Timestamp 型で持たせる必要がある |
| 匿名ユーザーの自動削除 | 未 | Authentication の設定にある |
| App Check | 未 | 誰でも匿名で書ける公開アプリなので、いたずら対策が要る |
| 利用規約・連絡先のページ | 未 | 自治体の情報セキュリティポリシーの審査で必ず聞かれる |

**技術より重いのは自治体のポリシー審査**、というのが先生と共有している認識。
「個人が用意した校外サーバーに児童生徒が入力する」形なので、そこが実質の関門になる。

---

## 6. 関連するもの

- 先生向け手引き（Artifact）: https://claude.ai/artifact/5xewohxh3AqvKmuCuSyGzD
- 公開URL: https://choppermoon1623-tech.github.io/ai-otazune/
- Firebase コンソール: https://console.firebase.google.com/project/ai-otazune/overview
- 電子教務手帳（`kyoumu-techou`）… **別プロジェクト**。分離前の残骸として、
  `kyoumu-techou/firestore.rules` に otazune のルールとテストデータが残っている可能性がある。
  新プロジェクトの動作確認後に消す予定（2026-09-19 時点で未実施）
